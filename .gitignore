# --- secrets (never commit) ---
.env
.env.*
!.env.example

# --- node / SvelteKit (web app at repo root) ---
node_modules/
/.svelte-kit/
/build/
/package/
vite.config.js.timestamp-*

# --- python (sync/) ---
.venv/
__pycache__/
*.pyc

# --- local caches / generated data (rebuild with sync/tspdt_sync.py) ---
.postercache/
tspdt.db
tspdt.db-shm
tspdt.db-wal
StartingList.xls

# --- local tooling ---
.claude/
