{
  "name": "films-desktop",
  "private": true,
  "version": "0.1.10",
  "description": "Tauri desktop wrapper for films.chrisj.uk — opens films in mpv / the OS default player.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
